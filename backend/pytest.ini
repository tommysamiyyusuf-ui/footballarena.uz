[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: tests that spawn threads or hit the DB concurrently
